use chrono::{DateTime, Duration, Utc};
use rustc_hash::FxHashMap;

use crate::{Bar, bar::Point};

// ── StrategyCtx ───────────────────────────────────────────────────────────────
#[derive(Debug, Clone)]
pub struct Strategy {
}
